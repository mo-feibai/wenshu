---
import { statusText, supersededText } from '../lib/helpers';
const { status, superseded } = Astro.props;
---
{superseded
  ? <span class="badge superseded">{supersededText}</span>
  : <span class={`badge ${status}`}>{statusText[status] ?? status}</span>}
