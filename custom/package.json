{
  "name": "custom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "python3 scripts/sync_files.py && python3 scripts/gen_site_content.py && astro build && pagefind --site dist",
    "build:win": "python scripts/sync_files.py && python scripts/gen_site_content.py && astro build && pagefind --site dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
